# Prompt Change Log

Each line is a one-line summary of a user-authored direction. AI outputs are supporting material, not the canonical spine.

```diff
+ Start from the missing artifact: GitHub tracks code, but nothing tracks how ideas arrive.
+ Use Karpathy's LLMWiki gist as the first imagined source artifact.
+ Make four origin agents explore compression, pedagogy, tooling, and canon.
+ Make killed branches visible because pruned thought is evidence.
+ Write a 2-minute OpenAI hackathon demo script around the product.
+ Introduce Ralph Loop as the technique: generate, critique, regenerate, diverge, kill, loop.
+ Define loopThing as the artifact: a portable exploration tree.
+ Use the demo question "What could documentation be when agents read it?"
+ Force divergence with constraint vectors: covenant, conversation, test suite, theatre.
+ Collapse the product loop into EXPLORE, JUDGE, DEEPEN, and SEAL.
+ Cut the hackathon build down to one real .loopthing file, one viewer, one canonical, and a small tree.
+ Pivot to a static OpenClaw viewer because it could ship quickly in-browser.
+ Make the file .loopthing instead of .json.
+ Refine the one-liner around "file format for AI work."
- Kill the idea that you LoopThing a GitHub repo.
+ Reframe the unit as an exploratory AI session that can produce repos, slides, docs, prototypes, and code.
+ Make .loopthing a single portable container, not an unpacked folder.
+ Use obvious primitives: prompts, follow-ups, discarded ideas, rough drafts, screenshots, docs, slides, prototypes, and code.
+ Make generated explainers a core output, not a side effect.
+ Add richer media so the artifact does not look like a markdown dump.
+ Put "Why It Exists" first and make the README screenshots tell the product story.
```
